package com.photo.shoot.model.db.enums; // Package declaration for enum classes

public enum Status { // Declaration of the Status enum
    ACTIVE, // Enum constant representing active status
    INACTIVE // Enum constant representing inactive status
}